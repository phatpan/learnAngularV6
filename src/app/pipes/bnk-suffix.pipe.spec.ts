import { BnkSuffixPipe } from './bnk-suffix.pipe';

describe('BnkSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new BnkSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
